Please update the Vue Router setup in `src/router/index.js` with the following routes:

- `/login` → LoginPage.vue
- `/signup` → SignupPage.vue
- `/dashboard` → DashboardPage.vue
- `/case/:id` → CasePage.vue
- `/complete` → CompletionPage.vue

Each route should use lazy-loaded `defineAsyncComponent()` syntax if possible.

📄 Create the corresponding page files in `src/pages/` with placeholder text like:
- `<h1>Login Page</h1>`, etc.

📦 Create a shared layout component:
- `src/components/MainLayout.vue`
- Includes a PrimeVue topbar and a `<router-view />` inside the layout

🧩 Wrap all pages (except `/login` and `/signup`) with MainLayout using route `meta.requiresAuth` and a layout wrapper system if necessary.

Make sure router is already integrated in `main.js`.
