import { defineStore } from 'pinia';

// Placeholder assessment store; legacy logic removed during migration to
// assignment-based AssessmentNew model. Re-implement when new UX is ready.
export const useAssessmentStore = defineStore('assessment', () => {
  return {};
});
